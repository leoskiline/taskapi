# Copie para .env e ajuste. O .env está no .gitignore — este exemplo existe
# para documentar as variáveis sem nunca commitar valor real.
DATABASE_URL=postgres://taskapi:taskapi@localhost:5432/taskapi?sslmode=disable
PORT=8080
LOG_LEVEL=info
SHUTDOWN_TIMEOUT=10s
